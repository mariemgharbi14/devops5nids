pipeline {
    agent any

    stages {
        stage('maven') {
            steps {
                sh """mvn -version"""
            }
        }
        stage ('MVN SONAR SONAR') {
            steps {
            echo "Maven SONAR SONAR";
                sh 'mvn sonar:sonar';
}
    }
}
